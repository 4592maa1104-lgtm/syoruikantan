# 引継ぎ資料（音声業務管理アプリ / syoruikantan）

最終更新: 2026-09-19
公開URL: https://syoruikantan.netlify.app

これまで作成した各種ドキュメント（`docs/`配下、`AI要約機能_デプロイ手順.md`等）と、
実際に起きたトラブル対応の内容をまとめた、引継ぎ用の1枚もの資料です。
細かい仕様は元のドキュメントも参照してください（本資料末尾に一覧あり）。

---

## 1. このアプリは何か

建築業の一人親方（まなみさん）が、現場からスマホで「話しかける」だけで
見積書・請求書・レシート・経費・スケジュールなどの事務作業を完結できるようにするアプリ。

**最重要目的**: パソコンに座る時間・入力作業そのものを減らすこと。
「フォームを並べる業務システム」ではなく「話しかけるアプリ」を目指す。

詳細な要件・設計思想は [docs/requirements.md](docs/requirements.md) を参照（全24項目、最重要ドキュメント）。

---

## 2. 技術構成・公開状況

| 項目 | 内容 |
|---|---|
| 形態 | PWA（ビルド不要の素のHTML/CSS/JS、`index.html`1ファイル） |
| ソース管理 | GitHub: `github.com/4592maa1104-lgtm/syoruikantan` |
| ホスティング | Netlify（無料枠）: `syoruikantan.netlify.app`、GitHub連携で自動デプロイ |
| データ保存 | ブラウザの`localStorage`（端末内完結、複数端末同期はしない） |
| バックエンド | Supabase（無料枠）を一時導入したが**現在は無効化**（詳細は4章） |
| AI | 導入コード自体はあるが**現在は無効・未使用**（詳細は4章） |
| 音声認識 | Web Speech API（ブラウザ標準、Android Chromeのみ対応） |

### アカウント情報（重要）

3つの外部サービスすべて、**同一のメールアドレス `4592maa1104@gmail.com`** で登録されている
（GitHubアカウント名は`4592maa1104-lgtm`）。パスワードは本資料には書きません
（Microsoft Edgeのパスワードマネージャーに保存されています。`edge://settings/passwords`で確認可能）。

- **GitHub**: `4592maa1104@gmail.com` でログイン可能（2026-09にパスワード再設定済み）
- **Netlify**: 同メールアドレスの「メール+パスワード」ログイン（GitHub連携は未完了、`Settings → Connected accounts`から連携可能）
- **Supabase**: 同メールアドレスのはずだが、**ログイン未確認・プロジェクト自体が消滅済みの可能性が高い**（4章参照）

---

## 3. 実装済み機能

- 話す・入力 → 構造化（現在はルールベースの簡易抽出。AI要約は無効、4章参照）→ 確認 → 保存
- 顧客・案件管理（案件を中心に、見積・請求・入金・経費・写真・予定をすべて紐付け）
- 見積書・請求書・領収書の作成（実物のExcelテンプレートPDFに合わせたレイアウトで実装済み、`window.print()`でPDF化、Excelダウンロードは数値のみ）
- **追加見積書**: 追加工事発生時に、元の見積書番号+`-A1`のような連番で追加見積書を作成。承認待ち/承認済みのステータス管理。請求書作成時に承認済みの追加見積を自動で候補表示
- 入金管理（分割入金・履歴・取消対応）
- レシート撮影 → 簡易OCR（Tesseract.js無料版）→ 経費登録。**複数枚まとめて選択して連続登録**も可能
- スケジュール管理（予定を案件に紐付け）
- チャット型業務アシスタント（下部ナビ「💬チャット」）:
  - 「今日/明日/8月13日の予定は？」のような日付質問
  - 「未請求の案件は？」「未入金は？」のような案件名なしの横断質問
  - 「○○邸どうなってる？」「いくら入金済み？」等、案件ごとの質問
  - 「今月いくら請求した/入金された？」の月次集計
  - 回答は文章＋案件名タップで詳細へ飛べるカード形式
  - `interpretQuestion`（質問解析）→`crossSearchData`（横断検索）→`buildProjectSnapshot`（案件統合）→`generateAnswer`（回答生成）の4層構造（旧ロジック・重複ロジックは削除済み）

コードは「データ操作層 → 業務ロジック層(Action) → Query層 → Chat Command Layer → UI/イベント層」に分離されている（`index.html`内、コメントで区切り済み）。

---

## 4. Supabase・AI機能について（現在無効化中・重要）

2026年8月頃、複数端末同期を見据えてSupabase連携（ログイン機能＋二重保存）が実装された。
さらにAI要約機能（Claude API Haiku経由、Supabase Edge Functions使用）も追加されたが、
**いずれも現在は無効化されている**。

### 何が起きたか（2026-09-19時点の調査結果）

1. アプリのログイン画面が、Supabaseサーバー（`krnzmxncbihmcidikehb.supabase.co`）に
   繋がらなくなり（`net::ERR_NAME_NOT_RESOLVED`＝そのサーバー自体が見つからない）、
   ログインもできず、アプリ自体が開けない状態になった
2. **原因は「パスワード忘れ」ではなく、Supabaseの無料プロジェクトが自動的に
   一時停止／削除されていたため**と推測される（無料枠は長期間使わないと消える仕様）
3. 応急処置として、`index.html`内の`SUPABASE_URL`/`SUPABASE_ANON_KEY`を空文字にし、
   ログイン確認処理自体を通らないようにした（元の値はコード内にコメントで残してある）
4. これにより、Supabaseへの二重保存・AI要約機能も、呼び出し条件（`isSupabaseConfigured()`）を
   満たさなくなり、自動的に無効化されている（コード自体は残っているが動かない）
5. 同時期、Netlifyの無料枠（デプロイ用クレジット）を使い切っており、GitHubにpushしても
   本番サイトに反映されない状態にもなっていた（`Skipped due to account credit usage exceeded`）。
   こちらは翌月に枠がリセットされ、手動で再デプロイ（`Trigger deploy`）して解消した

### 今後Supabase/AI機能を再開する場合

- Supabaseは新規にプロジェクトを作り直す必要がある可能性が高い（要確認）
- AI要約機能（`supabase/functions/ai-structure/index.ts`）はAnthropic API（**有料**）を使う設計。
  手順は [AI要約機能_デプロイ手順.md](AI要約機能_デプロイ手順.md) を参照。
  **課金判断（APIキー発行・支払い設定）は必ずユーザー自身が行うこと**（CLAUDE.mdの最優先ルール）
- 2026-09時点で、ユーザーからは「APIキーの課金は使わない方向で」との明確な意思表示あり

---

## 5. 未確定・今後の課題

[docs/requirements.md](docs/requirements.md) 21章「現時点で未確定の事項」より：

- 請求書の送付方法
- 顧客・元請け・下請けの管理方法
- 手伝ってくれる仲間の管理範囲
- 経費をどこまで管理するか
- 税理士・確定申告との連携範囲
- 入金管理の方法の細部
- 工程管理の詳細
- 領収書の税務上の正式な扱い（電子帳簿保存法・収入印紙の実物貼付は未対応）

その他:
- ホーム画面の「🎙 話す・入力」機能に既知の不具合あり（別途対応予定、スコープ外として保留中だった）
- Supabase複数端末同期・AI要約機能の再開判断（上記4章）
- 実機での一連の動作確認（顧客・案件登録→見積→追加工事→請求→入金確認）は未実施

---

## 6. 関連ドキュメント一覧

| ファイル | 内容 |
|---|---|
| [CLAUDE.md](CLAUDE.md) | 開発方針・ガードレール（課金禁止・セキュリティ最優先など）。作業前に必読 |
| [docs/requirements.md](docs/requirements.md) | 詳細要件定義（全24項目）。仕様判断の一次情報源 |
| [docs/phase1-implementation-report.md](docs/phase1-implementation-report.md) | チャット4層構造の実装報告（実装済み・内容は本資料3章に反映済み） |
| [docs/chat-architecture-phase1.md](docs/chat-architecture-phase1.md) | チャット機能の設計資料（開発時の検討過程） |
| [docs/chat-architecture-unified.md](docs/chat-architecture-unified.md) | チャット機能の設計資料（統合版、開発時の検討過程） |
| [docs/chat-redesign-plan.md](docs/chat-redesign-plan.md) | チャット再設計の検討資料（AI導入時のNetlify Functions案等を含む、開発時の検討過程） |
| [AI要約機能_デプロイ手順.md](AI要約機能_デプロイ手順.md) | AI要約機能（有料API）のデプロイ手順。4章参照 |
| [supabase/schema.sql](supabase/schema.sql) | Supabase用のテーブル・RLSポリシー定義（プロジェクト作り直し時に再利用可） |

**注意**: `docs/chat-architecture-*.md`と`docs/chat-redesign-plan.md`は開発途中の検討資料であり、
最終的な実装内容と完全には一致しない箇所がある。実装の正はあくまで`index.html`本体。
