# AI要約機能のデプロイ手順

この手順は、まなみさんご自身でも、PC側のClaude Codeに「この手順書の通りに進めて」と
そのまま渡していただいても構いません。ターミナル操作が含まれるので、Claude Codeに
任せる方が簡単だと思います。

作業前に：この手順は「有料サービスの契約」「APIキーの発行」を含みます。
①だけは必ずまなみさんご自身が行ってください（Claude Codeが代わりに発行することはできません）。

---

## ① Anthropic APIキーを取得する（要・課金設定）

1. https://console.anthropic.com/ を開き、アカウントを作成する（まだの場合）
2. 支払い方法（クレジットカード）を登録する
3. 左メニューの「Settings」→「Limits」から、**使用上限額を設定する**
   （例：月5ドルなど、安心できる金額。これ以上は自動的に使えなくなる設定です）
4. 「Settings」→「API Keys」→「Create Key」でキーを発行する
5. 表示されたキー（`sk-ant-...`という文字列）をコピーし、パスワード管理ツールなど
   安全な場所に保管する（このキーは絶対に人に見せたり、コードに直接書いたりしない）

---

## ② Edge Functionのコードを配置する

このセッションからすでに、次のファイルをプロジェクトフォルダに書き込み済みです。

```
supabase/functions/ai-structure/index.ts
```

中身の説明：「話す・入力」で話した文章を受け取り、Claude API（Haiku、一番安いモデル）に
要約・構造化を依頼して、結果を返すだけの小さなプログラムです。APIキーはこの中には
書かれておらず、次の③で安全な場所に登録します。

---

## ③ ターミナルで、Supabase CLIを使ってデプロイする

PowerShellまたはClaude Codeで、プロジェクトフォルダ（`C:\Users\tennma\Desktop\Aiの作業場`）
で以下を順番に実行してください。**すべて「読むだけ・設定するだけ」の安全な操作で、
削除や既存データへの変更は一切ありません。**

```powershell
# 1. Supabaseにログインする（ブラウザが開くので、いつものアカウントでログイン）
npx supabase login

# 2. このプロジェクトとひもづける（krnzmxncbihmcidikehbは既存のSupabaseプロジェクトID）
npx supabase link --project-ref krnzmxncbihmcidikehb

# 3. ①で取得したAPIキーを、Supabase側に安全に登録する
#    （sk-ant-... の部分を、①でコピーした実際のキーに置き換えてください）
npx supabase secrets set ANTHROPIC_API_KEY=sk-ant-ここに実際のキーを貼り付け

# 4. Edge Functionを配置する
npx supabase functions deploy ai-structure
```

`npx`が初回実行時に「Supabase CLIをインストールしますか？」のように聞いてくることが
あります。これは公式ツールのダウンロードなので、Yesで進めて問題ありません。

---

## ④ 動作確認

1. アプリ（syoruikantan.netlify.app）を開き、ログインする
2. 「🎙️ 話す・入力」から、何か文章を入力または話す
3. 「整理する」を押す（少し待つ：1〜3秒程度）
4. 確認画面の上部が「AIによる要約の下書きです」と表示されれば、成功です
5. もし「簡易な自動抜き出しです（AI要約は使えませんでした）」と出た場合は、
   ③のデプロイがうまくいっていないか、電波が悪い可能性があります。その場合は
   このチャットで「AI要約が動かない」と教えてください

---

## 費用の目安

- 1回の要約あたり：だいたい1円未満〜数円程度
- 月に100回使っても、数百円程度の見込みです
- ①で上限額を設定しているので、想定外に高額請求される心配はありません
