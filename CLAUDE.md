# 建築業向け・音声業務管理アプリ

一人親方の建築業者が、スマートフォンに「話しかける」だけで見積書・請求書・レシート・経費・スケジュールなどの事務作業を完結できるようにするアプリ。

**形態: ネイティブアプリ(Expo)ではなく PWA（Webアプリ）。** GitHub + Netlify（どちらも無料枠）のみで開発・公開・運用が完結する構成に切り替え済み。Node.jsのローカルインストールは必須ではない（ビルド不要な素のHTML/CSS/JSで進めている）。ホーム画面に追加してアプリのように使う想定で、App Store/Google Playへの配信は行わない。動作確認の主対象端末はAndroid（Web Speech APIの音声認識がChromeで動作するため。iPhone Safariは音声認識非対応で挙動が異なる点に注意）。

**最重要目的**: パソコンに座る時間をなくし、入力作業そのものを減らすこと。

詳細な要件・背景・未確定事項は [docs/requirements.md](docs/requirements.md) を必ず参照すること。仕様判断に迷ったらこのファイルを読み直す。

## 開発方針（最優先ルール）: 課金の禁止

**このプロジェクトでは、有料サービス・有料API・有料ライブラリを一切使用しない。** 他のどのガードレールよりも優先する。

- 新しいサービス・API・ライブラリを追加する前に、必ず無料枠内で完結できるか確認する
- 無料枠に制限がある場合は、その制限内で設計する（呼び出し回数・保存容量・月間上限など）
- 有料プランへの登録、クレジットカード情報の入力が必要な選択肢は提案・実装しない
- 「有料にすればもっと簡単/高機能にできる」場合でも、勝手に有料側を実装せず、無料の代替案を提示して確認を取ってから進める
- これは妥協ではなく意図した設計方針。過去のアプリ（みみより）も課金なしで実用に足るものを作れている

## 開発方針（最優先ルール）: セキュリティ

**顧客の個人情報・金額・入金情報などの機微データを扱うアプリのため、セキュリティ脆弱性には最大限の注意を払う。** 課金禁止と並ぶ最優先ルール。

- **クレジットカード情報は自前で保持・処理しない。** 決済機能を将来検討する場合も、カード番号等を自社サーバー/DBに保存する実装は絶対に行わず、必ずユーザーに確認してから進める
- **個人情報（顧客名・住所・電話番号・メール・案件内容・金額・振込先など）の漏洩経路を常に意識する。** 具体的には:
  - APIキー・シークレットはフロントエンド（PWA側のJS）に埋め込まない。将来バックエンドを導入する際は必ずサーバー側で保持する。`.env`等はgitignoreし、リポジトリや会話ログに秘密情報を残さない
  - 将来DBを導入する際（Supabase等）は Row Level Security (RLS) を必ず設定する。開発検証用の暫定的な緩いポリシーを使う場合は一時措置と明記し、認証導入時に必ず厳格化する（[docs/requirements.md](docs/requirements.md) 20章参照）
  - ログ・エラーメッセージ・デバッグ出力に個人情報や機密情報をそのまま出力しない
  - 依存ライブラリは既知の脆弱性がないか気にかけ、疑わしい場合はユーザーに確認する
- **重大なセキュリティリスクを伴う実装は、気づいた時点で独断で進めず、必ずユーザーに確認してから対応する。**

## 技術スタック

- **フロントエンド**: PWA（プレーンなHTML/CSS/JS。ビルドツール・フレームワーク不要を基本とし、必要になった場合のみ無料枠で完結する範囲で検討）
- **ソース管理**: GitHub（無料枠）
- **ホスティング**: Netlify（無料枠。GitHub連携でpush時に自動デプロイ、HTTPS配信によりマイク/カメラ等のブラウザAPIが利用可能）
- **バックエンド/DB**: 未確定。現在はブラウザのlocalStorageのみで完結（単一端末限定）。複数端末同期やAI連携が必要になった段階で、Supabase無料枠 or Netlify Functions無料枠を検討し、実装前にユーザーに確認する
- **AI**: 未確定。Claude API・OpenAI等の恒常的な有料APIは使わない方針。Gemini API無料枠、Groq無料枠など「課金なしで使えるLLM」を候補として検討し、実装前に必ずユーザーに確認する。現時点ではAI呼び出し自体を行わずルールベースのモック実装で進めている
- **音声認識**: Web Speech API（ブラウザ標準・無料）。Netlify配信のHTTPS環境で動作。対応状況はブラウザ依存（Android Chromeは対応、iPhone Safariは非対応）
- **帳票出力**: ブラウザ標準の印刷機能（`window.print()`）でPDF化。既存Excelテンプレートのレイアウトは未反映（実物提供後に反映）。追加ライブラリなしを基本方針とする

※ 上記は初期方針。実装を進める中で変更が必要になったら、まずユーザーに確認する。

## 絶対に守るガードレール

1. **課金の禁止（上記参照）を最優先で守る。**
2. **セキュリティ方針（上記参照）を最優先で守る。** 個人情報・機密情報の漏洩経路には常に注意する。
3. **APIキーをフロントエンド（PWA側のJS）に埋め込まない。** AI呼び出しを実装する際は必ずサーバー側を経由する。
4. **AIの推測をそのまま正式データとして確定しない。** 特に金額・数量・単価・顧客名・日付・請求先・振込先・税額は、AI下書き → ユーザー確認・修正 → 確定、の流れを必ず経由させる。
5. **既存の見積書・請求書フォーマットを維持する。** 新しいデザインの帳票を勝手に作らない。
6. **同じ情報の再入力をユーザーに求めない。** 見積→請求など、既存データから引き継げる部分は自動で引き継ぐ。
7. **フォームを並べる一般的な業務システムのUIにしない。** 音声・写真・最小限のタップを優先し、ホーム画面は主要機能のみのシンプルな構成にする。
8. **[docs/requirements.md](docs/requirements.md) の「21. 現時点で未確定の事項」にある項目は仕様を勝手に決めない。** ヒアリング・実物確認後にユーザーと確定する。
9. **運転中の操作を前提としたUI設計はしない。** 停車中・安全な場所での利用が前提。

## データモデルの骨格

```
顧客 → 案件 → (予定・工程 / 見積書 → 請求書 → 入金 / レシート・領収書・経費 / メモ・写真・関係者)
```

案件を中心にすべてを紐付ける。「○○邸の請求書どうなってる？」のような横断的な質問に答えられる構造にする。

## 開発フェーズ

一度に全機能を作らない。上から順に完成させる。

1. 音声入力 → AI整理 → 確認 → 保存
2. 顧客・案件管理
3. 見積書作成
4. 見積 → 請求書
5. レシート撮影 → 経費登録
6. スケジュール・工程管理
7. 入金管理・検索・分析・その他

現在地: **アーキテクチャをネイティブアプリ(Expo)からPWAに変更（GitHub+Netlifyで開発〜公開が完結。GitHub連携済み、`syoruikantan.netlify.app`で公開中）。** プロトタイプ（[prototype/index.html](prototype/index.html)）で「入力→AI整理(仮のルールベース処理)→確認→保存」に加え、音声入力(Web Speech API)・レシート撮影+簡易OCR(Tesseract.js無料版、経費用)・見積書/請求書/領収書の作成（すべて実際のExcelテンプレート(PDF提供)に合わせたレイアウト、ブラウザ印刷でPDF化、Excelダウンロードは数値のみ）まで一通り体験できる状態。

**顧客・案件を中心としたデータ連携（Track A・localStorage版）を実装済み**: 顧客/案件/追加工事/よく使う工事項目/入金（分割対応）/現場写真/予定をlocalStorageで管理し、見積書・請求書・領収書・経費レシート・話す入力メモ・写真・予定をすべて`projectId`で案件に紐付け可能。案件詳細ページで一元的に確認でき、見積→請求書作成時には未請求の追加工事を選んで請求に組み込めるチェックリスト、請求書には入金記録（分割入金・履歴・取消に対応、旧来の簡易ステータスとも後方互換）を追加。ホーム画面に「今日の予定」を表示。

**コードは「データ操作層 → 業務ロジック層(Action) → Query層 → Chat Command Layer → UI/イベント層」に分離**（`prototype/index.html`内、各層は明示的にコメントで区切られたセクションにまとめてある）。業務ロジック層の関数（`createChangeOrder`/`createDocumentRecord`/`recordPayment`/`createPhotoRecord`/`createScheduleEvent`等）はDOMに一切触れず、画面からも将来のAIからも同じ関数を呼び出せる設計。既存の書類データ構造・印刷/Excel出力ロジックは変更せず「スナップショットで顧客名等を保持＋projectIdで追加的に紐付け」という設計で後方互換を維持。

**チャット型業務アシスタント（Slice 1）を実装済み**: ホーム画面と下部ナビ（🏠ホーム/💬チャット/🏗案件/📄書類）からアクセスできるチャット画面を追加。「今日/明日/日付指定の予定」「未請求/未入金/請求済み/入金済み」「今月いくら請求/入金した？」「○○邸どうなってる？」「田中さんの案件」「何を買った？」等の質問に、Query層の関数を呼び出すルールベースの`interpretChatMessage`（自然文→intent）→`executeChatQuery`（intent→回答）で応答。予定検索は`schedule_events_v1`だけでなく「話す入力」メモの日付欄（`resolveCaptureDateToIso`で相対表現も解決）も横断して探す。案件名が「顧客・案件」に正式登録されていなくても、メモ・見積・請求の自由テキストから拾って回答する救済ルート（`findFreeTextProjectMatches`/`project_status_loose`）あり。「○○邸の請求書作って」は請求書案をチャット内に表示し、「請求書を確認」ボタンで既存の請求書作成画面（下書き済み）へ遷移する設計（いきなり確定はしない）。チャット専用のデータストアは作らず、既存データを読むだけ。チャット履歴はモジュール変数保持（永続化なし）。ルールベースのため未対応の言い回しは依然として多く、包括的な自然言語理解にはAI導入（Track B）が必要。

AI(Gemini等)導入時は`interpretChatMessage`をAIによる意図解釈に差し替えるだけでよい設計（Query/Actionの構造化命令を受け取る境界は`executeChatQuery`とAction層の関数群）。人工・買い出し・納品書、複合長文の自動分解、Supabase移行は未着手（Slice 2 / Track B）。

次のステップ: 実機での一連の動作確認（顧客・案件登録→見積→追加工事→請求→入金確認）、および領収書の税務上の正式な扱い（収入印紙・電子帳簿保存法）の確認。
